/* import React, { Component } from "react";
import Button from "./Button";

class Buttons extends Component {
  state = {
    buttons: [
      { className: "button one", value: "1" },
      { className: "button two", value: "2" },
      { className: "button three", value: "3" },
      { className: "button four", value: "4" },
      { className: "button five", value: "5" },
      { className: "button six", value: "6" },
      { className: "button seven", value: "7" },
      { className: "button eight", value: "8" },
      { className: "button nine", value: "9" },
      { className: "button zero", value: "0" },
      { className: "button clear", value: "clear" },
      { className: "button enter", value: "enter" },
    ],
  };

  render() {
    return (
      <div>
        {this.state.buttons.map((button) => (
          <Button key={button.value} className={button.className} value={button.value} button={button}></Button>
        ))}
      </div>
    );
  }
}

export default Buttons;
 */
